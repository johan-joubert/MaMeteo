import React from 'react'
import { View, TextInput, Button } from 'react-native'

class Accueil extends React.Component {
    render() {
        return (
            <View>
                <TextInput placeholder='Ville' />
                <Button title='ville' onPress={() =>{}}/>
            </View>
        )
    }
}